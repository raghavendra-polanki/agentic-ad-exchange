[project]
name = "aax-agents"
version = "0.1.0"
description = "AAX Sample Agents"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28",
    "pyyaml>=6.0",
    "anthropic>=0.52",
    "openai>=1.70",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
