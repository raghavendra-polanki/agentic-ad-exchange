[project]
name = "aax-sample-agent"
version = "0.1.0"
description = "Sample self-hosted agent for AAX — proves Path B onboarding"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "httpx>=0.27.0",
    "uvicorn>=0.30.0",
]
